<h2>All the players</h2>


<% @players.each do |player| %>

  <p> <%= link_to player.name, player_path(player)%>, <%= player.team.name %> </p>
<% end %>

<%= link_to "Create a player", new_player_path %>
